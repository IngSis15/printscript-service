spring:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect

services:
  asset:
    url: http://mocked-url-for-test

stream:
  lint:
    key: "lint"
  format:
    key: "format"
  status:
    key: "status"